#pragma once

#include <array>
#include "Utilities.hpp"

struct Map
{

//    const Utils::Point mapsize;

    //std::array<std::array<int, mapsize.x>, mapsize.y> mapdata;

public:
    Map();// : mapsize(20, 20) {};
    ~Map();
};

