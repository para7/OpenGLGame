#include "Title.h"

#include "Inputs.hpp"

Title::Title()
{
}


Title::~Title()
{
}

void Title::update()
{
    if (Input::IsClicked('a') || Input::IsClicked('b') || Input::IsClicked('c'))
    {

    }
}